---
title: "Testing DemoR in Ioslides"
author: Kelly Bodwin
date: 
output: 
  ioslides_presentation:
    fontsize: 10pt
    incremental: true
  # beamer_presentation:
  #   theme: "boxes"
  #   colortheme: "beaver"
  #   fonttheme: "structurebold"
---

```{r, include = FALSE}
knitr::opts_chunk$set(fig.width=6, fig.height=4, fig.align = 'center', fig.path='Figs/',
                      echo=TRUE, warning=FALSE, message=FALSE,eval=TRUE)

library(tidyverse)
library(demoR)

```



# Let's learn about cars

####gonna have to define my own pre class to override syntax coloring I think :(

## mtcars ftw

```{r, echo = FALSE}
demo_code('data(mtcars)') %>% hlt_regexp("mtcars") %>% hlt_funs(color = "red")

demo_code('summary(mtcars)') %>% hlt_regexp("mtcars")%>% hlt_funs(color = "red")

```

## math?

```{r}
demo_code('mean(1:10)') %>% hlt_regexp("mtcars")%>% hlt_funs(color = "red")
```
